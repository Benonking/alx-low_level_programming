#include "main.h"

/**
 * main - multiply to integers
 * @argc: argument count
 * @argv: array of arguments
 *
 * Return: 0 always
 */

int main (int argc, char **argv)
{
	int i;

	if (argc > 2)
	{
		printf("Error\n");
		exit(98);
	}
	for (i = 1; argv[i]; i++)
		
		num = atoi(argv[i])
}

