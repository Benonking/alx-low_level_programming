#include "lists.h"

/**
 * print_listint_safe - print a linked lsit
 * @head: head of list
 * Return: Return list or exit status 98 if fail
 */

size_t print_listint_safe(const listint_t *head)
{
	size_t i;

	if (head == NULL)
		exit 98;
	for (i = 0; )
}
