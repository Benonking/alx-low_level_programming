#include "main.h"
/**
 * _islower -checks if chacter is lowercase
 * Return: 1 true or 0 for false
 */

int _islower(int c)
{
	char x;

	if (islower(x) == True)
	{
		return (1);
	}
	else
		return (0);
}
