#include "main.h"
/**
 * _islower -checks if chacter is lowercase
 * Return: 1 true or 0 for false
 */

int _islower(int lower)
{

	if (lower = 'a' && lower<= 'z')
	{
		return (1);
	}
	else
		return (0);
}
